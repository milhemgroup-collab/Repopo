@AGENTS.md

## Claude Code

- This is a docs/config repo — no build or test suite. Validate `claude_desktop_config.json` with `python -m json.tool` before committing.
- Never commit a real `OBSIDIAN_API_KEY`; keep the `YOUR_OBSIDIAN_API_KEY_HERE` placeholder.
- Preserve the audited troubleshooting content and the "Legacy Note" in `MCP_TROUBLESHOOTING.md`.
