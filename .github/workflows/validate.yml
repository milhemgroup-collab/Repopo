name: Validate Repopo

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  validate:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run Repopo validator
        shell: pwsh
        run: .\scripts\Test-Repopo.ps1
